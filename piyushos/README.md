# PiyushOS

Original OS-style bio site built with React + TypeScript + Vite.

## Deploy to Vercel

1) Import the GitHub repo in Vercel.
2) Framework preset: **Vite** (or **Other** if needed).
3) Build command: `npm run build`
4) Output directory: `dist`
5) Node version: leave default.
